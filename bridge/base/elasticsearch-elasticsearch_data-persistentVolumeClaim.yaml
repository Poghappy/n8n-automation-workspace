#! elasticsearch-elasticsearch_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: elasticsearch-elasticsearch-data
    namespace: n8n-
    labels:
        com.docker.compose.service: elasticsearch
        com.docker.compose.volume: elasticsearch_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
